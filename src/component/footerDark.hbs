<div class='footerDark'>
  {{#with data}}
    <div class='footer'>
      {{#each pages}}
        <a href='{{path}}' class='footerLink'>{{name}}</a>
      {{/each}}
    </div>
    <span class='footer_copyright'>{{copyright}}</span>
  {{/with}}
</div>